import React from 'react'
import styled from 'styled-components'

const CalendarSection = () => {
  return <ContainerStyle>CalendarSection</ContainerStyle>
}

export default CalendarSection

const ContainerStyle = styled.section`
  height: 100vh;
`
